//! The `recall` binary.
//!
//! Argument parsing, output, and exit codes only: this crate composes the
//! implementations and owns presentation, while the behaviour lives in the
//! crates it depends on.

fn main() {
    // Command parsing lands in #5.
}
